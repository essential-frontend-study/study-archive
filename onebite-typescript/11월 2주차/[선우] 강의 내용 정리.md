# 🎯 [한 입 크기로 잘라 먹는 타입스크립트] - 섹션 6: 클래스
> 수강일: 2025.10.28

---

## 🧩 섹션 개요
- **섹션 목표:**
  - 자바스크립트의 클래스 개념을 이해하고, 타입스크립트에서의 클래스 문법 차이를 익힌다.
  - 접근 제어자와 인터페이스를 이용한 클래스 설계 방식을 학습한다.
- **핵심 학습 포인트:**
  - 자바스크립트와 타입스크립트의 클래스 차이
  - 클래스와 인스턴스의 개념
  - 상속과 `super()` 호출 원리
  - 접근 제어자
  - 클래스와 인터페이스의 결합
---

## 📌 강의별 정리

### 🔹 강의 1: 자바스크립트의 클래스 소개
- **핵심 내용:**
  - ES6부터 도입된 클래스 문법은 사실상 프로토타입 기반 상속을 더 직관적으로 표현한 문법
  - 클래스 내부에는 생성자(`constructor`)와 메서드가 포함될 수 있다.
  - `extends` 키워드로 상속을 구현하고, `super()`로 부모 클래스의 생성자 호출 가능
  - `tsconfig.json`의 `allowJS` 옵션은 JS 파일도 TS 프로젝트 내에서 허용할지 여부를 제어
- **코드 예제:**
  ```js
  class Student {
    // 생성자
    constructor(name, grade, age) {
      this.name = name;
      this.grade = grade;
      this.age = age;
    }

    study() {
      console.log('열심히 공부 함');
    }

    introduce() {
      console.log(`안녕하세요 ${this.name} 입니다!`);
    }
  }

  // 상속
  class StudentDeveloper extends Student {
    constructor(name, grade, age, favoriteSkill) {
      super(name, grade, age);
      this.favoriteSkill = favoriteSkill;
    }
    programming() {
      console.log(`${this.favoriteSkill}로 프로그래밍 함`);
    }
  }

  // 클래스를 이용해서 만든 객체 -> 인스턴스
  let student = new Student('이정환', 'A+', 27);
  student.study();
  student.introduce();

  const studentDeveloper = new StudentDeveloper('홍길동', 'B+', 27, 'TypeScript');
  console.log(studentDeveloper);
  studentDeveloper.introduce();
  studentDeveloper.programming();
  ```
- **내가 이해한 포인트:**
  - 자바스크립트의 클래스는 Syntatic Sugar로, 내부적으로는 프로토타입 상속이다.
  - `super()`는 부모의 생성자에 접근할 때 반드시 호출해야 하며, 자식 클래스의 `this`보다 먼저 실행되어야 한다.
---

### 🔹 강의 2: 타입스크립트의 클래스
- **핵심 내용:**
  - 타입스크립트의 클래스는 자바스크립트의 클래스 문법에 타입 시스템이 결합된 형태
  - 클래스 자체가 **값으로도, 타입으로도 사용**된다.
  - `tsconfig.json`의 `noImplicitAny` 옵션은 암시적 `any`를 허용할지 여불르 결정한다.
  - 클래스는 인터페이스처럼 객체 구조를 나타낼 수도 있다.
- **코드 예제:**
  ```ts
  class Employee {
    name;
    age;
    position;

    constructor(name: string, age: number, position: string) {
      this.name = name;
      this.age = age;
      this.position = position;
    }

    work() {
      console.log('일함');
    }
  }

  const employee = new Employee('이정환', 27, '사원');
  console.log(employee);

  // 타입스크립트에서의 클래스는 자바스크립트 클래스로 취급되는 동시에 타입으로도 취급된다.
  const employee2: Employee = {
    name: '',
    age: 0,
    position: '',
    work: () => {},
  };

  // 클래스 확장
  class ExecutiveOfficer extends Employee {
    officeNumber: number;

    constructor(
      name: string,
      age: number,
      position: string,
      officeNumber: number
    ) {
      super(name, age, position);
      this.officeNumber = officeNumber;
    }
  }
  ```
- **내가 이해한 포인트:**
  - 타입스크립트의 클래스는 클래스이자 동시에 타입으로도 사용될 수 있다.
---

### 🔹 강의 3: 접근 제어자
- **핵심 내용:**
  - 타입스크립트에서만 제공되는 문법이며, 코드의 캡슐화를 지원한다.
  - 종류:
    - `public`: 기본값으로 해당 필드는 어디서든 접근 가능
    - `private`: 클래스 내부에서만 접근 가능
    - `protected`: 클래스 내부 + 파생 클래스에서 접근 가능
  - 생성자 매개변수에 접근 제어자를 붙이면 필드 정의와 초기화를 동시에 처리할 수 있다.
- **코드 예제:**
  ```ts
  class Employee {
    //   private name;
    //   protected age;
    //   position;

    //   constructor(name: string, age: number, position: string) {
    //     this.name = name;
    //     this.age = age;
    //     this.position = position;
    //   }

    // 생성자에 접근 제어자 사용 시 필드 자동 생성하므로 필드 정의 따로 할 필요 없음
    constructor(
      private name: string,
      protected age: number,
      public position: string
    ) {}

    work() {
      console.log('일함');
    }
  }

  class ExecutiveOfficer extends Employee {
    officeNumber: number;

    constructor(
      name: string,
      age: number,
      position: string,
      officeNumber: number
    ) {
      super(name, age, position);
      this.officeNumber = officeNumber;
    }

    func() {
      // this.name  // ❌ private 필드는 파생 클래스에서도 액세스 할 수 없음
      console.log(`${this.age} 살 입니다.`); // ✅ protected 는 파생 클래스에서 액세스 할 수 있음
    }
  }

  const employee = new Employee('이정환', 27, 'developer');
  // employee.name = '홍길동'; // ❌ private 필드는 외부에서 접근 불가
  // employee.age = 30; // ❌ protected 필드는 클래스, 파생 클래스 외 접근 불가
  employee.position = '디자이너';
  ```
- **내가 이해한 포인트:**
  - 접근 제어자를 통해 내부 구현을 외부에서 보호할 수 있다.
  - `protected`는 상속 관계를 고려할 때 `private`보다 유연하다.
  - 생성자 매개변수에 접근 제어자를 붙이면 불필요한 필드 선언을 줄일 수 있다.
---

### 🔹 강의 4: 인터페이스와 클래스
- **핵심 내용:**
  - 클래스는 `implements` 키워드로 인터페이스를 구현할 수 있다.
  - 인터페이스는 클래스의 구조를 강제한다.
  - 인터페이스로 정의된 필드는 자동으로 `public` 취급된다.
  - `private` 필드가 필요한 경우 생성자 매개변수로 따로 정의해야 한다.
- **코드 예제:**
  ```ts
  interface CharacterInterface {
    name: string;
    moveSpeed: number;
    move(): void; // 호출 시그니처
  }

  // Character 클래스는 CharacterInterface를 구현한다.
  class Character implements CharacterInterface {
    // 인터페이스로 정의한 필드는 무조건 public
    // private 필드가 필요하다면 생성자 매개변수에 추가
    constructor(public name: string, public moveSpeed: number) {}

    move(): void {
      console.log(`${this.moveSpeed} 속도로 이동!`);
    }
  }
  ```
- **내가 이해한 포인트:**
  - `implements`는 상속이 아니라 구조 강제 역할을 한다.
  - 인터페이스로 클래스를 설계하면 일관성 있는 객체 생성이 가능하다.
  - 하나의 클래스가 여러 개의 인터페이스를 동시에 구현할 수도 있다.

---

# 🎯 [한 입 크기로 잘라 먹는 타입스크립트] - 섹션 7: 제네릭
> 수강일: 2025.10.28

---

## 🧩 섹션 개요
- **섹션 목표:**
  - 타입을 **동적으로 정의하고 재사용할 수 있는 제네릭(Generic)**의 개념을 이해한다.
  - 함수, 인터페이스, 클래스 등 다양한 구조에 제네릭을 적용하는 방법을 익힌다.
  - 제네릭을 통해 유연하면서도 타입 안정성을 유지하는 코드 작성법을 학습한다.
- ** 핵심 학습 포인트:**
  - 제네릭의 선언과 사용
  - 타입 변수 제한
  - 제네릭 인터페이스와 타입 별칭
  - 제네릭 클래스
  - Promise와 제네릭의 활용
---

## 📌 강의별 정리

### 🔹 강의 1: 제네릭 소개
- **핵심 내용:**
  - `제네릭(Generic)`은 타입을 매개변수화해서, 함수나 클래스가 다양한 타입에서 재사용될 수 있도록 만든 문법이다.
  - `<T>`와 같은 표현은 타입 변수(Type Parameter)이며, 호출 시점에 실제 타입이 결정된다.
  - 재사용성과 타입 안정성을 동시에 확보할 수 있다.
- **코드 예제:**
  ```ts
  // Generic: 일반적인, 포괄적인(General)

  // 제네릭 함수
  function func<T>(value: T): T {
    return value;
  }

  let num = func(10);			// T = number
  let bool = func(true);		// T = boolean
  let str = func('string');	// T = string

  // 타입 변수 직접 지정 가능
  let arr1 = func([1, 2, 3] as [number, number, number]); // 방법 1
  let arr2 = func<[number, number, number]>([1, 2, 3]); // 방법 2
  ```
- **내가 이해한 포인트:**
  - 제네릭은 함수의 입력 타입에 따라 반환 타입을 자동으로 추론한다.
  - 매개변수와 반환값의 타입이 연관되어 있을 때 `any` 대신 제네릭을 사용하는 것이 안전하다.
---

### 🔹 강의 2: 타입 변수 응용하기
- **핵심 내용:**
  - 제네릭은 여러 개의 타입 변수를 동시에 사용할 수 있다.
  - `extends` 키워드를 이용해 타입 변수의 범위를 제한할 수 있다.
- **코드 예제:**
  ```ts
  // 예시 1 - 타입 변수 2개 사용
  function swap<T, U>(a: T, b: U) {
    return [b, a];
  }
  const [a, b] = swap('1', 2);
 
  // 예시 2 - 튜플 타입에서 첫 번째 값 반환
  function returnFirstValue<T>(data: [T, ...unknown[]]) {
    return data[0];
  }
  let num = returnFirstValue([0, 1, 2]);
  let str = returnFirstValue([1, 'hello', 'mynameis']);

  // 예시 3 - extends로 타입 변수 타입 제한 
  function getLength<T extends { length: number }>(data: T) {
    return data.length;
  }
  let var1 = getLength([1, 2, 3]);
  let var2 = getLength('12345');
  let var3 = getLength({ length: 10 });
  // let var4 = getLength(10); // ❌ length 프로퍼티가 없음
  ```
- **내가 이해한 포인트:**
  - `extends`는 제네릭 타입이 특정 형태를 반드시 가지도록 제한한다.
  - 제한된 제네릭을 통해 타입 오류를 사전에 차단할 수 있다.
---

### 🔹 강의 3: map, forEach 메서드 타입 정의하기
- **핵심 내용:**
  - 내장 함수의 타입 구조를 직접 저의하며 제네릭 동작 원리를 익힌다.
  - 제네릭은 콜백 함수의 매개변수 타입까지 정확히 추론할 수 있다.
- **코드 예제:**
  ```ts
  /**
   * 타입스크립트로 내장함수 직접 구현하기
   */
  const arr = [1, 2, 3];
  const newArr = arr.map((item) => item * 2);

  // 1. map
  function map<T, U>(arr: T[], callback: (item: T) => T | U) {
    let result = [];
    for (const value of arr) {
      result.push(callback(value));
    }

    return result;
  }

  map(arr, (it) => it * 2);
  map(['hi', 'hello'], (it) => it.toUpperCase());
  map(['hi', 'hello'], (it) => parseInt(it));

  // 2. forEach
  function forEach<T>(arr: T[], callback: (item: T) => void) {
    for (const value of arr) {
      callback(value);
    }
  }

  forEach(arr, (it) => console.log(it));
  forEach(['123', '456'], (it) => console.log(it));
  ```
- **내가 이해한 포인트:**
  - 제네릭을 이용하면 콜백의 반환 타입까지 정적으로 추론할 수 있다.
  - 반환 타입이 입력 타입과 다를 수 있을 때, `<T, U>` 구조를 사용한다.

---

### 🔹 강의 4: 제네릭 인터페이스 & 제네릭 타입 별칭
- **핵심 내용:**
  - 인터페이스와 타입 별칭도 제네릭 타입을 가질 수 있다.
  - 인덱스 시그니처를 함께 사용하면 다양한 데이터 맵 구조 표현이 가능하다.
- **코드 예제:**
  ```ts
  interface KeyPair<K, V> {
    key: K;
    value: V;
  }

  // 제네릭 인터페이스를 사용할 때는 반드시 타입으로 정의할 때 타입 변수에 할당할 타입을 함께 작성해야 한다.
  let keyPair: KeyPair<string, number> = {
    key: 'key',
    value: 0,
  };

  let keyPair2: KeyPair<boolean, string[]> = {
    key: true,
    value: ['1'],
  };

  // 인덱스 시그니처 활용
  interface Map<V> {
    [key: string]: V;
  }

  let stringMap: Map<string> = {
    key: 'value',
  };

  let booleanMap: Map<boolean> = {
    key: true,
  };

  // 제네릭 타입 별칭
  type Map2<V> = {
    [key: string]: V;
  };

  let stringMap2: Map2<string> = {
    key: 'hello',
  };

  // 인터페이스 확장 예시
  interface Student {
    type: 'student';
    school: string;
  }

  interface Developer {
    type: 'developer';
    skill: string;
  }

  interface User<T> {
    name: string;
    profile: T;
  }

  function goToSchool(user: User<Student>) {
    const school = user.profile.school;
    console.log(`${school}로 등교 완료`);
  }

  const developerUser: User<Developer> = {
    name: '이정환',
    profile: {
      type: 'developer',
      skill: 'TypeScript',
    },
  };

  // goToSchool(developerUser); // ❌

  const studentUser: User<Student> = {
    name: '홍길동',
    profile: {
      type: 'student',
      school: '가톨릭대학교',
    },
  };
  ```
- **내가 이해한 포인트:**
  - 인터페이스/타입 별칭에 제네릭을 적용하면, 데이터 구조를 타입 레벨에서 재사용할 수 있다.
---

### 🔹 강의 5: 제네릭 클래스
- **핵심 내용:**
  - 클래스도 제네릭 타입을 가질 수 있다.
  - 클래스 내부에서 제네릭은 필드, 메서드 매개변수, 반환 타입 모두에 적용 가능하다.
- **코드 예제:**
  ```ts
  class List<T> {
    constructor(private list: T[]) {}

    push(data: T) {
      this.list.push(data);
    }

    pop() {
      return this.list.pop();
    }

    print() {
      console.log(this.list);
    }
  }

  const numberList = new List([1, 2, 3]);
  numberList.pop();
  numberList.push(4);
  numberList.print();
  ```
- **내가 이해한 포인트:**
  - 제네릭 클래스는 다양한 타입의 데이터를 안전하게 관리할 수 있다.
  - 내부 데이터 타입이 고정되지 않으므로 재사용성이 매우 높다.

---

### 🔹 강의 6: 프로미스와 제네릭
- **핵심 내용:**
  - `Promise<T>`는 제네릭의 대표적인 내장 예시이다.
  - 비동기 작업의 결과 타입을 `<T>`로 지정하면, `.then()`에서 안전하게 타입 추론이 가능하다.
  - `catch()`에서는 `err`가 기본적으로 `any` 타입이므로, 타입 좁히기가 필요하다.
- **코드 예제:**
  ```ts
  const promise = new Promise<number>((resolve, reject) => {
    setTimeout(() => {
      resolve(20);
    }, 3000);
  });

  promise.then((response) => console.log(response * 10));
  promise.catch((err) => {
    if (typeof err === 'string') {
      console.log(err);
    } else if (err instanceof Date) {
      console.log(err.getDate());
    }
  });

  // 프로미스를 반환하는 함수
  interface Post {
    id: number;
    title: string;
    content: string;
  }

  function fetchPost(): Promise<Post> {
    return new Promise((resolve, _) => {
      setTimeout(() => {
        resolve({
          id: 1,
          title: '게시글 제목',
          content: '게시글 컨텐츠',
        });
      }, 3000);
    });
  }

  const postRequest = fetchPost();
  postRequest.then((post) => {
    post.id;
  });
  ```
- **내가 이해한 포인트:**
  - `Promise<T>`의 제네릭 덕분에 비동기 함수의 반환 타입을 컴파일 타임에 보장할 수 있다.
---

# 🎯 [한 입 크기로 잘라 먹는 타입스크립트] - 섹션 8: 타입 조작하기
> 수강일: 2025.10.29

---

## 🧩 섹션 개요
- **섹션 목표:**
  - 타입스크립트에서 제공하는 다양한 타입 조작 기능을 이해하고, 이를 통해 기존 타입을 재활용하거나 가공하는 방법을 익힌다.
- **핵심 학습 포인트:**
  - 인덱스드 액세스 타입
  - `keyof` 연산자
  - 맵드 타입(Mapped Type)
  - 템플릿 리터럴 타입(Template Literal Type)

---

## 📌 강의별 정리

### 🔹 강의 1: 타입 조작하기
- **핵심 내용:**
  - 타입스크립트에서는 기존의 타입을 복사, 확장, 혹은 부분적으로 변형하여 새로운 타입을 정의할 수 있다.
  - 인터페이스나 타입 별칭으로 만든 타입을 유연하게 재활용하기 위한 여러 조작 문법이 존재한다.
  ![](https://velog.velcdn.com/images/iamsunwoo/post/a578ad06-7485-4301-a3fb-2510b5b0afcf/image.png) ![](https://velog.velcdn.com/images/iamsunwoo/post/dbec1949-f1fb-4309-ad00-e2cc1a1f3522/image.png)
- **내가 이해한 포인트:**
  - 단순히 타입을 선언하는 데 그치지 않고, 이미 존재하는 타입을 재사용 가능한 형태로 가공하는 것이 핵심이다.
  - 이후 배울 `keyof`, `in` 등의 문법이 이런 조작의 기본 도구가 된다.

---

### 🔹 강의 2: 인덱스드 액세스 타입
- **핵심 내용:**
  - 객체, 배열, 튜플 등 복합 타입에서 특정 프로퍼티나 요소의 타입만 추출할 수 있는 문법이다.
  - 문법
    ```text
    타입 별칭[프로퍼티 리터럴 타입]
    ```
  - 대괄호로 묶여진 부분을 인덱스라고 하는데, 인덱스에는 값이 아닌 타입(스트링 리터럴)이 들어간다.
  - 복잡한 타입 구조에서 필요한 부분만 추출할 때 유용하다.
  - `타입 별칭[숫자 또는 number]`로 배열 타입으로부터 하나의 요소 타입만 가져오는 것도 가능하다.
- **코드 예제:**
  - 객체 타입에서 추출
    ```ts
    interface Post {
      title: string;
      content: string;
      author: {
        id: number;
        name: string;
        age: number;
      };
    }

    function printAuthorInfo(author: Post['author']) {
      console.log(`${author.name}-${author.id}`);
    }

    // 객체의 특정 프로퍼티의 타입만 추출하는 것도 가능하다.
    function printAuthorAge(id: Post['author']['id']) {
      console.log(`${id}`);
    }
    ```
  - 배열 타입에서 추출
    ```ts
    type PostList = Post[];

    function printAuthorInfo(author: PostList[number]['author']) {
      console.log(`${author.id}-${author.name}`);
    }

    const post: PostList[0] = {
      title: '게시글 제목',
      content: '게시글 본문',
      author: {
        id: 1,
        name: '이정환',
        age: 27,
      },
    };
    ```
  - 튜플 타입에서 추출
    ```ts
    function indexedAccessTypeTuple() {
      type Tup = [number, string, boolean];
      type Tup0 = Tup[0];
      type Tup1 = Tup[1];
      type Tup2 = Tup[2];

      // 튜플 타입 내 모든 타입들의 최적의 공통 타입 추출
      type TupNum = Tup[number]; // type TupNum = number | string | boolean
    }
    ```
- **내가 이해한 포인트:**
  - 복잡한 타입에서 필요한 부분만 정확히 집어올 수 있다는 점이 강력하다.
- **실무/프로젝트 적용 아이디어:**
  - 백엔드에서 받아온 응답 타입 중 일부만 사용할 때 `Response['data']['user']` 식으로 안전하게 타입을 가져올 수 있다.

---

### 🔹 강의 3: keyof 연산자
- **핵심 내용:**
  - `keyof`는 **객체 타입**의 **프로퍼티** 이름을 **문자열 리터럴 타입으로 추출**한다.
  - 문법
    ```ts
    keyof 객체 타입
    ```
  - 이를 이용하면 프로퍼티 이름을 안전하게 참조할 수 있고, 런타임에서 존재하지 않는 키 접근을 방지할 수 있다.
  - `keyof`는 `typeof`와 함께 사용하면, 실제 객체의 타입에서 key만 추출하는 것도 가능하다.
  ![](https://velog.velcdn.com/images/iamsunwoo/post/5fc124ed-0134-4a0b-adc5-611d27c69c59/image.png)
- **코드 예제:**
  ```ts
  interface Person {
    name: string;
    age: number;
  }

  function getPropertyKey(person: Person, key: keyof Person) {
    return person[key]; // 인덱스드 액세스 타입
  }

  const person: Person = {
    name: '이정환',
    age: 27,
  };

  getPropertyKey(person, 'name');
  // getPropertyKey(person, 'name2'); // ❌ Person 타입에 name2 프로퍼티가 없으므로 타입 에러 발생

  // keyof + typeof 연산자
  type Employee = typeof employee; // type Employee = { name: string; age: number; }

  const employee = {
    name: '이정환',
    dept: 'IT',
  };

  function getPropertyKey2(employee: Employee, key: keyof typeof employee) {
    console.log(employee, key);
  }
  ```
- **내가 이해한 포인트:**
  - `keyof`는 단순히 키 목록 추출이 아니라, 타입 안정성을 유지하면서 동적 프로퍼티 접근을 가능하게 하는 도구다.
- **실무/프로젝트 적용 아이디어:**
  - Form 컴포넌트에서 입력 필드 이름이 객체 프로퍼티와 연결될 때, `keyof`를 사용하면 오타나 불일치 문제를 컴파일 타임에 잡을 수 있다.
    ```ts
    getValue<FormFields>(form, key: keyof FormFields)
    ```

---

### 🔹 강의 4: 맵드 타입
- **핵심 내용:**
  - 기존 객체 타입을 변형해 새로운 타입을 만드는 문법
  - 문법
    ```ts
    type NewType = {
      [key in keyof ExistingType]: NewValueType
    }
    ```
  - `in` 뒤에는 `keyof`로 추출한 프로퍼티 키들이 들어간다.
  - 인터페이스에서는 사용할 수 없고, 타입 별칭에서만 가능하다.
  - 자주 사용하는 기본 제공 맵드 타입으로는 `Partial<T>`, `Readonly<T>`, `Pick<T>`, `Record<K, T>` 등이 있다.
- **코드 예제:**
  ```ts
  interface User {
    id: number;
    name: string;
    age: number;
  }

  // 모든 프로퍼티를 선택적으로 변경
  type PartialUser = {
    [key in keyof User]?: User[key];
  };

  // 모든 프로퍼티를 boolean으로 변경
  type BooleanUser = {
    [key in keyof User]: boolean;
  };

  // 모든 프로퍼티를 읽기 전용으로 변경
  type ReadonlyUser = {
    readonly [key in keyof User]: User[key];
  };

  // 사용 예시
  function fetchUser(): ReadonlyUser {
    return { id: 1, name: '이정환', age: 27 };
  }

  function updateUser(user: PartialUser) {
    // 변경하고 싶은 프로퍼티만 전달 가능
    console.log(user);
  }

  updateUser({ age: 30 }); // ✅
  ```
- **내가 이해한 포인트:**
  - 맵드 타입은 기존 타입의 형태를 유지하면서 일괄 변환할 수 있어 타입 재사용성을 극대화한다.
  - 특히 `Partial`, `Pick`, `Omit` 같은 내장 타입의 동작 원리가 이 개념을 기반으로 한다.
- **실무/프로젝트 적용 아이디어:**
  - `Readonly`를 적용해, 서버 응답 데이터를 불변 객체로 처리한다.

---

### 🔹 강의 5: 템플릿 리터럴 타입
- **핵심 내용:**
  - 문자열 리터럴 타입을 기반으로 정해진 패턴의 문자열만 허용하는 타입을 정의할 수 있다.
  - `+` 연산자처럼 문자열을 결합하는 게 아니라, 가능한 조합을 모두 **유니온 타입**으로 확장한다.
  ![](https://velog.velcdn.com/images/iamsunwoo/post/0dac0588-4ef8-463d-b2ac-4ffaeff6e3a6/image.png)
- **코드 예제:**
  ```ts
  type Color = 'red' | 'black' | 'green';
  type Animal = 'dog' | 'cat' | 'chicken';

  // type ColoredAnimal = 'red-dog' | 'red-cat' | 'red-chicken' | 'black-dog'; // 💩
  type ColoredAnimal = `${Color}-${Animal}`;

  const coloredAnimal: ColoredAnimal = 'black-cat';
  ```
- **내가 이해한 포인트:**
  - 단순히 문자열을 제한하는 수준을 넘어, 패턴 기반의 타입 검증을 수행할 수 있다.
  - 정해진 접두사 등을 자동 완성에 반영할 수 있어 오타 방지와 일관성 유지에 큰 도움이 된다.
- **실무/프로젝트 적용 아이디어:**
  - Tailwind CSS나 className 패턴 관리 시 `"bg-${color}"`처럼 사용
  - API 엔드포인트 타입 정의
    ```ts
    type ApiRount = `/api/${'user' | 'post' | 'comment'}`;
    ```
---

# 🎯 [한 입 크기로 잘라 먹는 타입스크립트] - 섹션 9: 조건부 타입
> 수강일: 2025.10.29

---

## 🧩 섹션 개요
- **섹션 목표:**
  - 타입스크립트의 고급 기능 중 하나인 **조건부 타입**의 동작 원리를 이해하고, 이를 통해 타입을 상황에 따라 분기 처리하거나 자동으로 추론하는 방법을 익힌다.
- **핵심 학습 포인트:**
  - 조건부 타입의 기본 구조
  - 분산적 조건부 타입
  - `infer` 키워드를 활용한 타입 추론
  - 유틸리티 타입(`Exclude`, `Extract`, `ReturnType`, `Awaited`)의 동작 원리 이해
---

## 📌 강의별 정리

### 🔹 강의 1: 조건부 타입 소개
- **핵심 내용:**
  - 조건부 타입은 타입 간의 관계(**extends**)를 비교한 뒤, 결과에 따라 다른 타입을 반환하는 문법이다.
  - 문법
    ```ts
    T extends U ? X : Y
    ```
    - `T`가 `U`의 서브 타입이면 `X` 타입을 반환, 그렇지 않으면 `Y` 타입을 반환
  - 특히 제네릭과 결합할 때 강력한 타입 분기 처리가 가능하다.
- **코드 예제:**
  - 기본 조건부 타입
    ```ts
    type A = number extends string ? string : number; // number

    type ObjA = { a: number; };
    type ObjB = { a: number; b: number; };

    type B = ObjB extends ObjA ? number : string; // number
    ```
  - 제네릭 활용 조건부 타입
    ```ts
    type StringNumberSwitch<T> = T extends number ? string : number;
    let varA: StringNumberSwitch<number>; // string
    let varB: StringNumberSwitch<string>; // number
    ```
  - 오버로드 시그니처 + 조건부 타입
    ```ts
    // 함수 내부에서 조건부 타입의 결과를 알 수 없을 때 - 오버로드 시그니처 사용
    function removeSpaces<T>(text: T): T extends string ? string : undefined;
    function removeSpaces(text: any) {
      return text.replaceAll(' ', '');
    }

    let result = removeSpaces('hi im winterlood');
    result.toUpperCase();

    let result2 = removeSpaces(undefined);
    ```
- **내가 이해한 포인트:**
  - 조건부 타입은 단순한 분기문이 아니라, **타입 레벨에서 삼항 연산을 수행**하는 문법이다.
  - 제네릭과 함께 사용하면 타입의 형태나 제약 조건에 따라 자동으로 타입을 변환할 수 있다.

---

### 🔹 강의 2: 분산적인 조건부 타입
- **핵심 내용:**
  - 조건부 타입의 제네릭 매개변수 `T`에 **Union** 타입이 들어가면, 타입스크립트는 내부적으로 각 원소별로 조건을 **분리 평가**한다.
  - 즉, `A | B`가 들어오면 `A extends U ? X : Y`와 `B extends U ? X : Y`를 따로 평가하고, 그 결과를 다시 **Union으로 합친다.**
  - `never`는 공집합이므로, Union 연산에서 자동으로 제거된다.
- **코드 예제:**
  - 기본적인 분산 조건부 타입
    ```ts
    type StringNumberSwitch<T> = T extends number ? string : number;

    let a: StringNumberSwitch<number | string>; // number | string
    let d: StringNumberSwitch<boolean | number | string>; // number | string
    ```
  - 특정 타입 제외하기
    ```ts
    type Exclude<T, U> = T extends U ? never : T;
    type A = Exclude<number | string | boolean, string>; // number | boolean
    ```
  - 특정 타입만 추출하기
    ```ts
    type Extract<T, U> = T extends U ? T : never;
    type B = Extract<number | string | boolean, string>; // string
    ```
- **내가 이해한 포인트:**
  - 분산적 조건부 타입은 Union 타입과 함께 쓰일 때 각 요소별 타입 분기가 일어난다.
  - 즉, 타입 레벨의 반복문처럼 동작한다고 볼 수 있다.
- **실무/프로젝트 적용 아이디어:**
  - `Exclude`, `Extract` 등의 내장 유틸리 타입은 모두 이 개념을 활용한다.

---

### 🔹 강의 3: infer - 조건부 타입 내에서 타입 추론하기
- **핵심 내용:**
  - `infer`는 조건부 타입 내부에서 새로운 타입 변수를 추론할 때 사용한다.
  - `infer R` → "R이라는 타입을 추론한다" 라는 의미
  - 주로 함수의 반환 타입이나 Promise 내부 타입, 튜플의 특정 요소 타입을 추론할 때 쓰인다.
- **코드 예제:**
    ```ts
    type FuncA = () => string;
    type FuncB = () => number;

    // 반환값을 기준으로 타입 호환성을 판별할 때는 업캐스팅만 가능
    type ReturnType<T> = T extends () => string ? string : never;

    type A = ReturnType<FuncA>; // string
    type B = ReturnType<FuncB>; // never
    ```
  - infer를 활용한 일반화된 반환 타입 추론
    ```ts
    type ReturnType2<T> = T extends () => infer R ? R : never;

    type A2 = ReturnType2<FuncA>; // () => string extends () => R 해당 조건을 true가 되게 만들어 줌
    type B2 = ReturnType2<FuncB>;
    type C = ReturnType2<number>; // type: never 추론이 불가함
    ```
  - Promise의 결괏값 타입 추론
    ```ts
    type PromiseUnPack<T> = T extends Promise<infer R> ? R : never;
    type PromiseA = PromiseUnPack<Promise<number>>; // number
    ```
- **내가 이해한 포인트:**
  - `infer`는 함수나 제네릭 구조 안에서 자동으로 타입을 꺼내는 기능을 한다.
  - 타입을 일일이 명시하지 않아도, 타입스크립트가 구조를 보고 내부 타입을 추론해준다.
- **실무/프로젝트 적용 아이디어:**
  - `ReturnType`, `Awaited` 같은 내장 유틸리티 타입이 모두 `infer` 기반이다.
---

# 🎯 [한 입 크기로 잘라 먹는 타입스크립트] - 섹션 10: 유틸리티 타입
> 수강일: 2025.10.30

---

## 🧩 섹션 개요
- **섹션 목표:**
  - 타입스크립트가 제공하는 유틸리티 타입 활용법 익히기
  - 제네릭, Mapped Type, 조건부 타입을 활용해 타입을 응용하는 방식 이해
  - 실무에서 재사용 가능한 타입 패턴 습득
- **핵심 학습 포인트:**
  - Partial, Required, Readonly
  - Pick, Omit, Record
  - Exclude, Extract, ReturnType
  - 유틸리티 티입이 어떻게 구현되는지 원리 파악
---

## 📌 강의별 정리

### 🔹 강의 1: 유틸리티 타입 소개
- **핵심 내용:**
  - 유틸리티 타입은 TS가 제공하는 타입 재활용 툴킷
  - 내부적으로는 제네릭 + Mapped Type + 조건부 타입 조합으로 되어 있다.
  - 자주 쓰는 타입 변환 로직을 미리 만들어둔 것
    ![](https://velog.velcdn.com/images/iamsunwoo/post/565742f6-87db-4d63-82ac-ecdcd2b352e6/image.png)
- **내가 이해한 포인트:**
  - 유틸리티 타입들이 어떻게 동작하는지 이해하면 구현도 가능하다.
---

### 🔹 강의 2: 맵드 타입 기반의 유틸리티 타입 1- Partial, Required, Readonly
- **핵심 내용:**

  |타입|설명
  |:---|:---|
  |`Partial<T>`|객체 타입의 모든 속성을 optional로 변환|
  |`Required<T>`|객체 타입의 모든 속성을 필수로 변환(`-?`키 사용)|
  |`Readonly<T>`|객체 타입의 모든 속성을 읽기 전용으로 변환|
- **코드 예제:**
  - `Post` 타입
    ```ts
    type Post = {
      title: string;
      content: string;
      tags: string[];
      thumbnailURL?: string;
    }
    ```
  - `Partial`
    ```ts
    type Post = {
      title: string;
      content: string;
      tags: string[];
      thumbnailURL?: string;
    }

    type Partial<T> = {
      [K in keyof T]?: T[K]
    }

    const noTitlePost: Partial<Post> = {
      content: ""
    }
    ```
  - `Required`
    ```ts
    type Required<T> = {
        [K in keyof T]-?: T[K];
      };

    const withThumbnailPost: Required<Post> = {
      title: '한입 타스 추가',
      tags: ['ts'],
      content: '',
      thumbnailURL: 'https://', // ✅ 선택적 프로퍼티도 반드시 존재해야함
    };
    ```
  - `Readonly`
    ```ts
    type Readonly<T> = {
      readonly [K in keyof T]: T[K];
    };

    const readonlyPost: Readonly<Post> = {
      title: '보호된 게시글 입니다.',
      tags: [],
      content: '',
    };

    // readonlyPost.content = ''; // ❌
    ```
- **내가 이해한 포인트:**
  - 선택적/필수/읽기전용 같은 속성 레벨 제어가 Mapped Type 핵심 기능
  - Required는 optional 속성(`?`)을 없애기 위해 `-?` 연산 사용
---

### 🔹 강의 3: 맵드 타입 기반의 유틸리티 타입 2 - Pick, Omit, Record
- **핵심 내용:**

  |타입|설명|
  |:---|:---|
  |`Pick<T, K>`|특정 키만 골라서 새로운 타입 생성|
  |`Omit<T, K>`|특정 키 제외하고 새로운 타입 생성|
  |`Record<T, K>`|키 union → $\text{V}$ 타입을 갖는 객체 생성|
- **코드 예제:**
  - `Post` 타입
    ```ts
    type Post = {
      title: string;
      tags: string[];
      content: string;
      thumbnailURL?: string;
    }
    ```
  - `Pick`
    ```ts
    type Pick<T, K extends keyof T> = {
      [key in K]: T[key];
    };

    const lagacyPost: Pick<Post, 'title' | 'content'> = {
      title: '엣날 글',
      content: '옛날 컨텐츠 ',
    };
    ```
  - `Omit`
    ```ts
    type Omit<T, K extends keyof T> = Pick<T, Exclude<keyof T, K>>;

    const noTitlePost: Omit<Post, 'title'> = {
      tags: ['1', '2'],
      content: '요즘 컨텐츠',
    };
    ```
  - `Record`
    ```ts
    type ThumbnailLegacy = {
      large: {
        url: string;
      };
      medium: {
        url: string;
      };
      small: {
        url: string;
      };
    };

    type Record<K extends keyof any, V> = {
      [key in K]: V;
    };

    type Thumbnail = Record<'large' | 'medium' | 'small', { url: string }>;
    ```
- **내가 이해한 포인트:**
  - Pick, Omit, Record는 Mapped Type의 유틸리티 타입이다.
- **질문/토론거리:**
  - `Record` vs `Index Signature`
  
    |비교 항목|`Record<K, V>`|Index Signature(`[key: string]: V`)|
    |:---|:---|:---|
    |목적|특정 **키 집합**에 대해 타입 강제|**불특정 키**에 대해 타입 지정|
    |키|유니온 타입으로 지정된 키만 사용|모든 string/number key 허용|
    |안전성|타입이 더 엄격함(정확한 키 필요)|다소 느슨함(아무 키나 가능)|
    |사용 예|Enum, 특정 상태 매핑|자유로운 키-값 저장 객체|
---

### 🔹 강의 4: 조건부 타입 기반의 유틸리티 타입 - Exclude, Extract, ReturnType
- **핵심 내용:**

  |타입|설명|
  |:---|:---|
  |`Exclude<T, U>`|$\text{T}$에서 $\text{U}$ 제거|
  |`Extract<T, U>`|$\text{T}$에서 $\text{U}$만 추출|
  |`ReturnType<T>`|함수 반환 타입 추론(`infer` 사용)|
  
- **코드 예제:**
  - `Exclude`
    ```ts
    type Exclude<T, U> = T extends U ? never : T;
    type A = Exclude<number | string | boolean, boolean>;
    ```
  - `Extract`
    ```ts
    type Extract<T, U> = T extends U ? T : never;
    type B = Extract<number | string | boolean, number>;
    ```
  - `ReturnType`
    ```ts
    type ReturnType<T extends (...args: any) => any> = T extends (
      ...args: any
    ) => infer R
      ? R
      : never;
    function funcA() {
      return 'hello';
    }

    function funcB() {
      return 10;
    }

    type ReturnA = ReturnType<typeof funcA>; // ReturnType의 타입 변수로 함수 시그니처 전달
    type ReturnB = ReturnType<typeof funcB>;
    ```
- **내가 이해한 포인트:**
  - `infer`는 조건부 타입에서 타입을 추론할 때 사용하는 키워드
  - ReturnType은 함수 시그니처를 T로 넘겨야 하므로 `typeof` 사용
- **실무/프로젝트 적용 아이디어:**
  - 특정 union에서 사용하지 않은 상태 제거할 때 Exclude
- **질문/토론거리:**
  - `Exclude` vs `Omit`

    |비교 항목|`Exclude<T, U>`|`Omit<T, K>`|
    |:---|:---|:---|
    |역할|유니온 타입에서 특정 타입 제거|객체 타입에서 특정 key 제거|
    |사용 대상|**유니온 타입**|**객테 타입**|
    |반환|남는 타입 유니온|새로 구성된 객체 타입|
    |공식 구현|`T extends U ? never : T` |`Pick<T, Exclude<keyof T, K>`|
---


